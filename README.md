# tta-demo-app

## How to start?
### Install dependencies

First, install poetry:

```bash
pip install poetry
```

Next, install python packages with poetry:

```bash
poetry install
```

### Run App

```bash
make run-app
```
